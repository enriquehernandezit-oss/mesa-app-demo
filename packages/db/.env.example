# The pool is configured once here and exported to every consumer.
# Local dev points at a local Postgres; production uses the Railway URL.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/mesa
