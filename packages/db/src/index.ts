// @mesa/db — the single source of truth for the database.
//
// Milestone 1 fills this in:
//   - the pooled Postgres client (configured ONCE here, exported for every consumer)
//   - the Drizzle schema (packages/db/src/schema) imported by the API and, for
//     types only, by the app — this is the "typed end to end" guarantee.
//
// Nothing here yet; M0 only establishes the workspace and its type boundary.
export {}
