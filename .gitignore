node_modules/
.env
.env.*
!.env.example
dist/
build/
*.log
.DS_Store

# Machine-local Claude Code permissions
.claude/settings.local.json

# Capacitor native projects (generated)
apps/app/ios/
apps/app/android/
.capacitor/

# Drizzle
packages/db/drizzle/meta/_journal.json.bak

# Foursquare OS Places extract (Stage A output for the M6 importer) — large,
# regenerated from the source dataset, never committed.
packages/db/data/

# Google Places cache for the M5 Top 100 importer — regenerated by --refresh,
# and holds live API responses that shouldn't be replayed as fixtures.
# top100.json itself (the extract from the spreadsheet) IS committed.
apps/api/data/top100-google.json
